name: nurdaily
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.3
  http: ^1.6.0
  # supabase_flutter: ^2.12.0  # ← DISABLED untuk test

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

# Hapus dependency_overrides karena tidak perlu lagi
# dependency_overrides:
#   app_links: 6.4.1
